import user_model from "../../../models/db.js";
import user_repository from "../../../repositories/user_repository";

describe("when we try to create an user", () => {
  it("creates and returns the created user", async () => {});
});
